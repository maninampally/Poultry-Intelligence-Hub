{
  "name": "poultry-intelligence-hub",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const file of ['package-lock.json','yarn.lock']) { try { fs.rmSync(file, { force: true }); } catch {} } const ua = process.env.npm_config_user_agent || ''; if (!ua.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev:web": "corepack pnpm --filter @murgi-mitra/web dev",
    "dev:api": "corepack pnpm --filter @murgi-mitra/api dev",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./apps/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:migrate": "corepack pnpm --filter @murgi-mitra/db migrate",
    "codegen": "corepack pnpm --filter @murgi-mitra/api-spec codegen",
    "seed": "corepack pnpm --filter @murgi-mitra/scripts seed:murgi"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
